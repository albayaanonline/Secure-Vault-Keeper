openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: My Secrets - Premium Encrypted Vault API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: users
    description: User management
  - name: secrets
    description: Secret vault operations
  - name: categories
    description: Secret categories
  - name: activity
    description: Activity logs
  - name: dashboard
    description: Dashboard stats and summaries
  - name: storage
    description: Object storage upload and serving endpoints
  - name: vault-files
    description: Encrypted file vault management

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # User / profile
  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user profile
      security:
        - clerkAuth: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
    patch:
      operationId: updateMe
      tags: [users]
      summary: Update current user profile
      security:
        - clerkAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserProfileUpdate"
      responses:
        "200":
          description: Updated user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"

  # Categories
  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List all categories
      security:
        - clerkAuth: []
      responses:
        "200":
          description: List of categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
    post:
      operationId: createCategory
      tags: [categories]
      summary: Create a custom category
      security:
        - clerkAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"

  /categories/{id}:
    patch:
      operationId: updateCategory
      tags: [categories]
      summary: Update a category
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryUpdate"
      responses:
        "200":
          description: Updated category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
    delete:
      operationId: deleteCategory
      tags: [categories]
      summary: Delete a category
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # Secrets
  /secrets:
    get:
      operationId: listSecrets
      tags: [secrets]
      summary: List secrets (metadata only, no values)
      security:
        - clerkAuth: []
      parameters:
        - name: category
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: tag
          in: query
          schema:
            type: string
        - name: archived
          in: query
          schema:
            type: boolean
        - name: favorite
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: List of secrets (no encrypted values)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SecretMeta"
    post:
      operationId: createSecret
      tags: [secrets]
      summary: Create a new secret
      security:
        - clerkAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SecretInput"
      responses:
        "201":
          description: Created secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Secret"

  /secrets/{id}:
    get:
      operationId: getSecret
      tags: [secrets]
      summary: Get a single secret including encrypted value
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Secret with encrypted value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Secret"
        "404":
          description: Not found
    patch:
      operationId: updateSecret
      tags: [secrets]
      summary: Update a secret
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SecretUpdate"
      responses:
        "200":
          description: Updated secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Secret"
    delete:
      operationId: deleteSecret
      tags: [secrets]
      summary: Delete a secret
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /secrets/{id}/favorite:
    patch:
      operationId: toggleFavoriteSecret
      tags: [secrets]
      summary: Toggle favorite status
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretMeta"

  /secrets/{id}/archive:
    patch:
      operationId: toggleArchiveSecret
      tags: [secrets]
      summary: Toggle archive status
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretMeta"

  /secrets/{id}/access:
    post:
      operationId: recordSecretAccess
      tags: [secrets]
      summary: Record that a secret was viewed
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK

  # Activity logs
  /activity:
    get:
      operationId: listActivity
      tags: [activity]
      summary: List activity log entries
      security:
        - clerkAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
        - name: offset
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of activity entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityEntry"

  # Dashboard / stats
  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get dashboard statistics
      security:
        - clerkAuth: []
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /dashboard/recent-secrets:
    get:
      operationId: getRecentSecrets
      tags: [dashboard]
      summary: Get recently accessed secrets
      security:
        - clerkAuth: []
      responses:
        "200":
          description: Recently accessed secrets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SecretMeta"

  /dashboard/security-score:
    get:
      operationId: getSecurityScore
      tags: [dashboard]
      summary: Get vault security score
      security:
        - clerkAuth: []
      responses:
        "200":
          description: Security score details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecurityScore"

  # Object storage presigned upload
  /storage/uploads/request-url:
    post:
      tags: [storage]
      operationId: requestUploadUrl
      summary: Request a presigned URL for file upload
      security:
        - clerkAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadUrlRequest"
      responses:
        "200":
          description: Presigned upload URL generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadUrlResponse"
        "400":
          description: Missing or invalid required fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Failed to generate upload URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /storage/public-objects/{filePath}:
    get:
      tags: [storage]
      operationId: getPublicObject
      summary: Serve a public asset
      parameters:
        - name: filePath
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Object content
          content:
            "*/*":
              schema:
                type: string
                format: binary
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /storage/objects/{objectPath}:
    get:
      tags: [storage]
      operationId: getStorageObject
      summary: Serve a private object
      security:
        - clerkAuth: []
      parameters:
        - name: objectPath
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Object content
          content:
            "*/*":
              schema:
                type: string
                format: binary
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  # Vault files
  /vault-files:
    get:
      operationId: listVaultFiles
      tags: [vault-files]
      summary: List vault files (metadata only)
      security:
        - clerkAuth: []
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: contentType
          in: query
          schema:
            type: string
        - name: favorite
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: List of vault files
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VaultFile"
    post:
      operationId: createVaultFile
      tags: [vault-files]
      summary: Register an uploaded file in the vault
      security:
        - clerkAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VaultFileInput"
      responses:
        "201":
          description: Created vault file record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultFile"

  /vault-files/{id}:
    get:
      operationId: getVaultFile
      tags: [vault-files]
      summary: Get a vault file record
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vault file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultFile"
        "404":
          description: Not found
    patch:
      operationId: updateVaultFile
      tags: [vault-files]
      summary: Update vault file metadata
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VaultFileUpdate"
      responses:
        "200":
          description: Updated vault file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultFile"
    delete:
      operationId: deleteVaultFile
      tags: [vault-files]
      summary: Delete a vault file
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /vault-files/{id}/favorite:
    patch:
      operationId: toggleFavoriteVaultFile
      tags: [vault-files]
      summary: Toggle favorite status of a vault file
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated vault file
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultFile"

  /vault-files/{id}/download-url:
    get:
      operationId: getVaultFileDownloadUrl
      tags: [vault-files]
      summary: Get a signed download URL for a vault file
      security:
        - clerkAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Signed download URL
          content:
            application/json:
              schema:
                type: object
                required: [url]
                properties:
                  url:
                    type: string
        "404":
          description: Not found

components:
  securitySchemes:
    clerkAuth:
      type: http
      scheme: bearer

  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    UserProfile:
      type: object
      required: [id, clerkId, email, createdAt]
      properties:
        id:
          type: integer
        clerkId:
          type: string
        email:
          type: string
        displayName:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        vaultLocked:
          type: boolean
        autoLockMinutes:
          type: integer
        twoFactorEnabled:
          type: boolean
        createdAt:
          type: string

    UserProfileUpdate:
      type: object
      properties:
        displayName:
          type: string
        autoLockMinutes:
          type: integer

    Category:
      type: object
      required: [id, name, slug, icon, color, isSystem, secretCount]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        icon:
          type: string
        color:
          type: string
        isSystem:
          type: boolean
        secretCount:
          type: integer

    CategoryInput:
      type: object
      required: [name, icon, color]
      properties:
        name:
          type: string
        icon:
          type: string
        color:
          type: string

    CategoryUpdate:
      type: object
      properties:
        name:
          type: string
        icon:
          type: string
        color:
          type: string

    SecretMeta:
      type: object
      required: [id, title, categorySlug, categoryName, categoryIcon, categoryColor, isFavorite, isArchived, createdAt, updatedAt]
      properties:
        id:
          type: integer
        title:
          type: string
        categorySlug:
          type: string
        categoryName:
          type: string
        categoryIcon:
          type: string
        categoryColor:
          type: string
        description:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        isFavorite:
          type: boolean
        isArchived:
          type: boolean
        expiresAt:
          type: ["string", "null"]
        lastAccessedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string

    Secret:
      type: object
      required: [id, title, categorySlug, categoryName, categoryIcon, categoryColor, encryptedValue, isFavorite, isArchived, createdAt, updatedAt]
      properties:
        id:
          type: integer
        title:
          type: string
        categorySlug:
          type: string
        categoryName:
          type: string
        categoryIcon:
          type: string
        categoryColor:
          type: string
        encryptedValue:
          type: string
        description:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        isFavorite:
          type: boolean
        isArchived:
          type: boolean
        expiresAt:
          type: ["string", "null"]
        lastAccessedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string

    SecretInput:
      type: object
      required: [title, categoryId, encryptedValue]
      properties:
        title:
          type: string
        categoryId:
          type: integer
        encryptedValue:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string
        expiresAt:
          type: string

    SecretUpdate:
      type: object
      properties:
        title:
          type: string
        categoryId:
          type: integer
        encryptedValue:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string
        expiresAt:
          type: ["string", "null"]

    ActivityEntry:
      type: object
      required: [id, action, resourceType, createdAt]
      properties:
        id:
          type: integer
        action:
          type: string
        resourceType:
          type: string
        resourceId:
          type: ["integer", "null"]
        resourceTitle:
          type: ["string", "null"]
        ipAddress:
          type: ["string", "null"]
        userAgent:
          type: ["string", "null"]
        createdAt:
          type: string

    DashboardStats:
      type: object
      required: [totalSecrets, favoriteSecrets, archivedSecrets, expiringSecrets, categoryCounts, recentActivity]
      properties:
        totalSecrets:
          type: integer
        favoriteSecrets:
          type: integer
        archivedSecrets:
          type: integer
        expiringSecrets:
          type: integer
        categoryCounts:
          type: array
          items:
            $ref: "#/components/schemas/CategoryCount"
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/ActivityEntry"

    CategoryCount:
      type: object
      required: [categorySlug, categoryName, categoryIcon, categoryColor, count]
      properties:
        categorySlug:
          type: string
        categoryName:
          type: string
        categoryIcon:
          type: string
        categoryColor:
          type: string
        count:
          type: integer

    SecurityScore:
      type: object
      required: [score, grade, checks]
      properties:
        score:
          type: integer
        grade:
          type: string
        checks:
          type: array
          items:
            $ref: "#/components/schemas/SecurityCheck"

    SecurityCheck:
      type: object
      required: [name, passed, weight, description]
      properties:
        name:
          type: string
        passed:
          type: boolean
        weight:
          type: integer
        description:
          type: string

    UploadUrlRequest:
      type: object
      required: [name, size, contentType]
      properties:
        name:
          type: string
          minLength: 1
        size:
          type: integer
          minimum: 1
        contentType:
          type: string
          minLength: 1

    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL:
          type: string
          format: uri
        objectPath:
          type: string

    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: string

    VaultFile:
      type: object
      required: [id, name, originalName, objectPath, contentType, size, isFavorite, createdAt, updatedAt]
      properties:
        id:
          type: integer
        name:
          type: string
        originalName:
          type: string
        objectPath:
          type: string
        contentType:
          type: string
        size:
          type: integer
        description:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        isFavorite:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string

    VaultFileInput:
      type: object
      required: [name, objectPath, contentType, size, originalName]
      properties:
        name:
          type: string
        originalName:
          type: string
        objectPath:
          type: string
        contentType:
          type: string
        size:
          type: integer
        description:
          type: string
        tags:
          type: array
          items:
            type: string
        encryptedKey:
          type: string

    VaultFileUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string
